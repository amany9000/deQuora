# deQuora - the decentarlized quora

* Clone the repository

* Add truffle
* run `truffle develop`
* run `compile`
* run `migrate`

go to client folder
* cd `client`

Start `react app`
* `npm install`
* `npm start`
